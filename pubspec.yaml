name: app_manager
description: A new Flutter project.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  # path_provider: ^2.0.7
  android_intent_plus: ^5.1.0
  # Flutter 的一些动画
  flutter_spinkit: ^5.1.0

  dio: ^5.4.3
  # shortcut: ^0.0.1
  path: ^1.8.0
  get: ^4.1.3
  global_repository:
    git: https://github.com/nightmare-space/global_repository
  flutter_svg: ^1.1.6
  hive: ^2.0.4
  app_channel:
    git:
      url: https://github.com/nightmare-space/app_channel
  adbutil:
    git: https://github.com/nightmare-space/adbutil
  file_manager_view:
    git: https://github.com/nightmare-space/file_manager_view.git

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
